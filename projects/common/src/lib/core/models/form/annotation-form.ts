import { Annotation } from '@clrwdoc/common/core/models/annotation';
import { FormControl } from '@angular/forms';

/** Annotation form controls. */
export type AnnotationFormControls = FormControl<Annotation>;
